import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title:
    "NeuralGuru | AI-Powered KBAT Learning Platform for Malaysian Students",
  description:
    "NeuralGuru helps Malaysian students master KBAT (Higher Order Thinking Skills) questions through AI-powered analysis, adaptive practice, and MOE-aligned learning. Understand why you fail, not just what you fail.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
